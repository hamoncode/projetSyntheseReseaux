{
  "blocked_ips": ["192.168.1.10", "8.8.8.8"],
  "blocked_ports": [12345, 4444],
  "suspicious_keywords": [
    "curl -fsSL http://",
    "curl | sh",
    "wget http://",
    "wget -q -O -",
    "powershell -enc",
    "powershell.exe -nop -w hidden",
    "/bin/bash -i >& /dev/tcp",
    "nc -e /bin/sh",
    "bash -c \"bash -i\"",
    "user-agent: sqlmap",
    "union select",
    "' or 1=1 --",
    "select * from users where",
    "chmod 777",
    "system(\"",
    "eval(",
    "<?php system",
    "<script>alert(",
    "%3Cscript%3Ealert%28",
    "curl -X POST",
    "bash -i >& /dev/tcp",
    "rm -rf /",
    "scp -P",
    "python -c 'import socket",
    "0xdeadbeef",
    "set-cookie: jsessionid=",
    "user-agent: nikto",
    "cmd.exe /c whoami",
    "base64 -d <<<",
    "curl --insecure",
    "wget --no-check-certificate",
    "strcpy(",
    "python3 -m http.server",
    "user-agent: nessus",
    "nmap -sS",
    "nmap -A",
    "iptables -F",
    "pkill -f",
    "am i root",
    "reverse_tcp",
    "POST /api/v1/login",
    "GET /.env",
    "Authorization: Basic"
  ]
}
